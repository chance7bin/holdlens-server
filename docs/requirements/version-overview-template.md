# v0.0.1

## 迭代目标

## 范围内 PRD

| 模块 | PRD | 涉及端 | 状态 | 关联 OpenSpec Change |
| --- | --- | --- | --- | --- |

> `关联 OpenSpec Change` 是本项目 PRD 与本地 Change 映射的唯一维护位置。优先链接到本项目 `openspec/changes/<change-name>/proposal.md`；尚未创建时填写 `-`。

## 不包含范围

## 依赖与风险

## 决策记录
