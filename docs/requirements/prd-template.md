# 需求标题

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 所属版本 | `v0.0.1` |
| 模块 | 待补充 |
| 涉及端 | 用户端 / 管理端 / Agent / 跨端 |
| 状态 | 待确认 |
| 关联 OpenSpec Change | - |
| 最后更新 | YYYY-MM-DD |

## 1. 背景

说明为什么要做这件事，当前问题是什么。

## 2. 目标

明确这次需求希望达成的结果。

## 3. 范围

### 本次包含

- 待补充

### 本次不包含

- 待补充

### 产品影响范围

- 业务模块：
- 涉及端：
- 用户入口：
- 相关业务流程：
- 相关业务对象：

说明：本节只描述产品和业务视角的影响范围。接口、任务、数据模型、兼容性、迁移风险等研发影响评估放到 OpenSpec proposal 或 design 中。

## 4. 用户角色与主要流程

### 4.1 用户角色

- 角色 A：
- 角色 B：

### 4.2 用户故事

- 作为 `某类用户`，我希望 `完成某件事`，以便 `获得某种价值`。

### 4.3 主流程

1. 待补充

### 4.4 异常流程

- 待补充

### 4.5 状态流转

如涉及导入任务、资产匹配、持仓确认、风险审核等状态变化，在这里描述状态流转。

```text
初始状态 -> 中间状态 -> 结束状态
```

## 5. 需求明细

### 5.1 功能点 A

- 待补充

### 5.2 功能点 B

- 待补充

### 5.3 业务规则

- 待补充

### 5.4 业务数据约束

- 待补充

### 5.5 权限与可见性

- 可操作角色：
- 可查看角色：
- 不可操作或不可查看的场景：

## 6. 非功能要求

- 性能体验：
- 安全与合规：
- 可用性：
- 适用端与设备要求：

## 7. 验收标准

验收标准应尽量写成可验证场景。

- [ ] 场景 1：给定 `前置条件`，当 `用户操作`，则 `系统结果`。
- [ ] 场景 2：给定 `前置条件`，当 `异常操作`，则 `系统提示或拦截结果`。
- [ ] 场景 3：给定 `前置条件`，当 `边界条件`，则 `系统结果`。

## 8. 依赖与风险

- 依赖：
- 风险：

## 9. 未决问题

- [ ] 待确认

## 10. OpenSpec 衔接

- 是否需要创建 OpenSpec change：是 / 否
- 建议 change 名称：
- 需要进入 spec 的业务规则：
- 需要进入 spec 的验收标准：
- 需要研发在 proposal / design 中评估的问题：

## 11. 附件与参考

- 待补充
