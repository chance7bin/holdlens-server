<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echoamoy.holdlens.server.infrastructure.dao.IAssetRecordChangeDao">
    <insert id="insertBatch">
        INSERT INTO asset_record_change
            (operation_id, user_id, record_id, change_type, before_amount, after_amount, currency, before_status, after_status, operator_id)
        VALUES
        <foreach collection="changes" item="change" separator=",">
            (#{change.operationId}, #{change.userId}, #{change.recordId}, #{change.changeType},
             #{change.beforeAmount}, #{change.afterAmount}, #{change.currency},
             #{change.beforeStatus}, #{change.afterStatus}, #{change.operatorId})
        </foreach>
    </insert>
</mapper>
