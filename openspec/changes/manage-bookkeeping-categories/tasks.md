# 实施任务

## 1. 领域模型与用例

- [x] 1.1 新增统一类别定义、scope、配置状态和图标目录领域模型，替换固定枚举解析；验证：系统/用户类别、默认配置、名称字符数、类型和图标白名单 Domain 测试。
- [x] 1.2 扩展 Repository 端口与类别查询用例，合并系统默认和用户覆盖且 GET 不写库；验证：启用/未添加分组、顺序、活动账目数和用户隔离 Case 测试。
- [x] 1.3 实现幂等创建、启用、停用和完整集合重排；验证：重名、并发幂等、越权、集合不一致、末尾排序和重新启用不恢复账目测试。
- [x] 1.4 将类别停用、活动账目批量软删除和剩余顺序整理放入同一事务；验证：成功删除数、重复停用与任一步失败整体回滚测试。

## 2. 数据库与基础设施

- [x] 2.1 新增类别定义、用户配置的 PO、DAO、Repository 适配器和 MyBatis XML；验证：参数化 SQL、用户过滤、默认覆盖合并和批量软删除结构测试。
- [x] 2.2 更新初始化 SQL 并新增增量迁移，写入 23 个系统类别及默认状态；验证：既有 code 不变、已使用默认停用类别生成 ENABLED 覆盖、迁移不修改账目状态。
- [x] 2.3 为类别定义、用户配置、创建幂等和查询顺序建立必要唯一键与索引；验证：初始化与迁移结构一致，现有账目分类索引继续覆盖停用更新。

## 3. API 与共享契约

- [x] 3.1 扩展分类、条目和统计 DTO 的图标/来源字段，并新增设置、图标、创建、启用、停用、重排请求响应；验证：DTO 映射和错误语义测试。
- [x] 3.2 新增类别管理 GET/POST Controller 路径，保留现有分类查询兼容性；验证：HTTP 方法、参数绑定、幂等和明确动作路径测试。
- [x] 3.3 更新 `contracts/holdlens-server/client/bookkeeping.md`，明确表结构外的字段、状态、事务、排序、图标分组、停用删除和兼容语义；验证：服务端接口与共享契约逐项对照。

## 4. 既有记账链路适配

- [x] 4.1 创建和修订改为校验用户当前启用类别，条目详情与列表通过类别仓储返回名称和图标；验证：系统/自定义/停用/越权类别测试。
- [x] 4.2 分类统计改为解析系统或用户类别名称和图标，明细、统计和账单继续排除 DELETED 账目；验证：自定义类别排行和停用后聚合测试。

## 5. 质量门与安全

- [x] 5.1 运行 Domain、Case、Infrastructure、Trigger 相关测试并确认目标测试实际执行。
- [x] 5.2 按 runbook 使用 JDK 17 串行运行后端 Maven 聚合测试或编译。
- [x] 5.3 检查用户隔离、事务回滚、参数化 SQL、创建幂等，以及错误和日志不泄露金额、备注或其他用户关系。
- [x] 5.4 同步任务状态并运行 `openspec validate --strict manage-bookkeeping-categories`。
