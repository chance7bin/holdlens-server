## 1. 基线与迁移

- [x] 1.1 从基金净值历史基线 DDL 移除重复普通索引。
- [x] 1.2 新增仅删除重复普通索引的可重复执行前向迁移。

## 2. 验证

- [x] 2.1 补充数据库结构测试，覆盖基线唯一索引和前向迁移的目标索引。
- [x] 2.2 运行数据库结构定向测试并严格验证 OpenSpec change。
- [x] 2.3 使用兼容当前 MySQL 的索引存在性判断与动态 DDL 替代不支持的 `DROP INDEX IF EXISTS`，并更新结构回归测试。
