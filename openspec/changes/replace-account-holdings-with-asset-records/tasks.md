## 1. 决策、契约与初始化

- [ ] 1.1 确认基金、股票系统目录的最终初始层级，并更新系统目录初始化清单；验证：评审目录树、稳定编码和金额方向。
- [ ] 1.2 更新根目录客户端契约，覆盖资产目录、资产记录、自选内部引用、拆分和多币种汇总；验证：请求、响应、错误和兼容语义完整。
- [ ] 1.3 更新受 ADR-005 影响的既有 ADR/OpenSpec 引用，避免继续把 `asset_info` 解释为自选或持仓资产主数据；验证：全文检索无活动事实源冲突。

## 2. 数据库模型

- [ ] 2.1 新增 `asset_catalog`、`asset_record`、`asset_record_change`、`watchlist_item`、`exchange_rate` DDL 和必要索引；验证：字段、唯一键、用户查询索引与本设计一致。
- [ ] 2.2 移除 `asset_account`、`asset_info`、`asset_holding`、`asset_holding_change`，重建开发 Mock 数据；验证：目标环境数据可重建确认和初始化脚本可重复执行。
- [ ] 2.3 为系统目录和初始最新汇率提供受控初始化；验证：系统编码唯一、系统目录不可变规则和汇率方向合法。

## 3. Domain 与 Infrastructure

- [ ] 3.1 实现资产目录、资产记录、资产变更、自选关系和汇率领域模型与 Repository 接口；验证：Domain 无基础设施依赖。
- [ ] 3.2 实现 PO、DAO、Repository 适配器和 Mapper XML；验证：SQL 参数化、用户隔离、状态和索引查询路径正确。
- [ ] 3.3 实现按 `asset_kind` 校验 `fund.id` / `stock_market.id` 的公共标的引用，确认公共刷新保持技术 ID 稳定；验证：不存在、类型冲突和停用场景测试。

## 4. 资产目录与资产记录用例

- [ ] 4.1 实现系统/用户目录查询和用户目录创建、修改、软删除；验证：两级、叶子、父子方向、所有权和系统只读场景测试。
- [ ] 4.2 实现普通金额、未细分基金/股票和具体持仓创建；验证：三态、名称生成/复制、不可变字段、重复标的记录和零金额测试。
- [ ] 4.3 实现金额更新、归档、恢复和删除并追加变更历史；验证：名称/备注不写历史，金额和状态变化只追加历史。
- [ ] 4.4 实现单目标拆分事务和并发控制；验证：部分拆分、全量拆分、同目录同币种、重复标的新建、超额与并发回滚测试。

## 5. 自选与刷新目标调整

- [ ] 5.1 将批量加入、自选列表、搜索状态和移除能力迁移到 `watchlist_item`；验证：幂等、用户隔离、公共标的校验和无资产副作用。
- [ ] 5.2 保持外部 `assetRef` 不透明契约并映射内部技术 ID；验证：基金、A 股、美股、非法引用和类型冲突测试。
- [ ] 5.3 调整基金刷新目标选择为自选基金与基金资产记录的去重并集，不再读取 `asset_info`；验证：未细分基金无具体 ID 时不产生错误公共目标，具体基金正确入选。

## 6. 汇率与资产汇总

- [ ] 6.1 实现当前汇率写入和查询，限制为外币兑人民币；验证：正向合法、人民币作基准和外币交叉汇率被拒绝。
- [ ] 6.2 实现按目标币种的当前资产汇总；验证：同币种、外币兑人民币、人民币兑外币、外币交叉换算和金额方向测试。
- [ ] 6.3 实现汇率缺失的部分汇总语义；验证：缺失币种和原币金额完整返回，无法换算记录不按零处理。

## 7. API、测试与质量门

- [ ] 7.1 新增或调整 API DTO、服务接口和 Controller，客户端不接触数据库技术 ID；验证：协议字段、错误响应和兼容性测试。
- [ ] 7.2 覆盖 Domain、Case、Infrastructure、Trigger 单元和集成测试；验证：相关 Maven 测试与编译通过。
- [ ] 7.3 检查资产金额、名称、备注和用户关系不进入日志、任务参数或错误摘要；验证：安全评审和日志断言。
- [ ] 7.4 运行 `openspec validate --strict replace-account-holdings-with-asset-records`，根据真实实现和验证结果同步任务状态。
