# 任务

- [x] 1. 更新 OpenSpec 和跨项目契约，明确 A 股/美股的 `currency` 与 `volume_unit` 语义。
- [x] 2. 扩展 server API Request、Case Command、Domain Entity、PO、Repository 映射和 MyBatis XML。
- [x] 3. 更新 `stock_market` 初始化 SQL，新增 `currency` 与 `volume_unit` 字段。
- [x] 4. 补充/更新 server 单元测试，覆盖 A 股与美股单位落库。
- [x] 5. 运行 server 相关测试和 `openspec validate --strict add-stock-market-units`。
